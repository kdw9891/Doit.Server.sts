<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="RegisterInsert">

  <select id="Register" resultType="HashMap" parameterType="HashMap">
	INSERT INTO tb_user (
        user_id
    ,   password
    ,   user_nickname
    ,   email
    ,   use_yn
    ,   create_dt
    ) VALUES (
        #{user_id}
    ,   SHA2(#{password}, 256)
    ,   #{user_nickname}
    ,   #{email}
    ,   'Y'
    ,   CURRENT_TIMESTAMP()
    )
  </select>
  
</mapper>