<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Home">

  <select id="UserHome" resultType="HashMap" parameterType="HashMap">
    SELECT 
      	h.user_id
	,	h.cat_level
    ,  	h.xp_total
    ,	hp.total_points
    FROM 
      tb_user_cat h
    LEFT JOIN 
      tb_total_points points ON 
      h.user_id = hp.user_id
    WHERE
      h.user_id = #{user_id}
  </select>
</mapper>
